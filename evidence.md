Old Commit Hash: _18dd2a375b1d8c8a86c906d4df04ca0826acf8eb_________

New Commit Hash: __40f948ca9ede99a57ef2b31586844f1a05c006b8________

The commit hash was different after updating the commit message.

In Git, a commit hash is computed using the commit’s content, message, and related metadata.
When the commit message is altered, Git generates a completely new commit with a different hash rather than modifying the existing one.

This demonstrates that Git commits are immutable and cannot be directly altered once they are created.
